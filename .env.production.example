# ============================================================
# .env.production  (використовується під час npm run build)
# ============================================================

# У production логуємо лише WARN та вище (не показуємо DEBUG/INFO)
VITE_LOG_LEVEL=WARN

VITE_APP_VERSION=1.0.0
